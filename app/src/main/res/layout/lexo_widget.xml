<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/lexo_widget.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/widget_background"
    android:padding="14dp">

    <!-- Número grande de pendientes -->
    <TextView
        android:id="@+id/widget_due"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:textSize="38sp"
        android:textStyle="bold"
        android:textColor="#EF9F27"/>

    <TextView
        android:id="@+id/widget_label_due"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="pendientes"
        android:textSize="12sp"
        android:textColor="#9DA8BB"
        android:layout_marginBottom="8dp"/>

    <!-- Separador -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#2A3245"
        android:layout_marginBottom="8dp"/>

    <!-- Total palabras -->
    <TextView
        android:id="@+id/widget_total"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0 palabras guardadas"
        android:textSize="11sp"
        android:textColor="#9DA8BB"/>

    <!-- Racha -->
    <TextView
        android:id="@+id/widget_streak"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔥  0 días de racha"
        android:textSize="11sp"
        android:textColor="#9DA8BB"
        android:layout_marginTop="3dp"/>

</LinearLayout>
